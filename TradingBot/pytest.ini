[pytest]
testpaths = app/tests
python_files = test_*.py
asyncio_mode = auto
markers =
    integration: slow, touches live feed.db / spawns workers. Excluded from the
        pre-push gate (the fast deterministic core); run with `-m integration`
        or preflight.ps1 -Full.
filterwarnings =
    ignore::DeprecationWarning
